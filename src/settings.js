// settings.js

const settings = {
    skipIntro: true,       // Skips boot crawl animation
    instantText: true,     // All terminal text prints instantly
    darkMode: false,       // For future UI theme toggle
    enableCRT: true        // Turn CRT scanline/glow effects on/off
  };
  
  export default settings;
  